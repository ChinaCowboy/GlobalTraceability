export * from './search/search.service';

// The reason for creating this file is so you can import multiple classes on a single line rather than
// having to import each individual class on separate lines.